spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:/D:/PROJECTS/PERSONAL/microservices-demo/config-server-repository
          clone-on-start: true
      fail-fast: true